function mostrar()
{
	var contador = 1 ;

	while (contador <= 10) 
	{
		console.log(contador) ;

		contador += 1 ;
	}
 /* el incremento a la variable se lo escribe += "nuemro" ,
  para asignarle una funacion para qe no se repite la condicion
   infinita veces o ++ o -- para que el incremento o decremento sea de valor 1 */


}//FIN DE LA FUNCIÓN